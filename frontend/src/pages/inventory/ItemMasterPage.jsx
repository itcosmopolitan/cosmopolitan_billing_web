import ItemsPage from './ItemsPage'

/** Central item catalog — global master data, branch listing & default pricing. */
export default function ItemMasterPage() {
  return <ItemsPage mode="master" />
}
